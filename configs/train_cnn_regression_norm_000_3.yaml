# configs/train_cnn_regression_norm.yaml
model:
  name: cnn_dual_branch
  embedding:
    mode: linear
    dim: 8
    share_between_utr5_utr3: false
  trunk:
    channels: [64, 128, 256]
logging:
  out_dir: outputs/cnn_v1_reg
  checkpoint_dir: outputs/cnn_v1_reg/checkpoints
  save_every_epochs: 0
  csv_log: logs/cnn_v1_reg_training.csv
task: regression
data:

  dataset_dir: data/processed/seq_cnn_v1_reg
train:
  epochs: 10
  batch_size: 384 #256
  standardize_y: true
  grad_clip: 1.0
  huber_beta: 1.0
  num_workers: 12
  resume: outputs/cnn_v1_reg_old_/checkpoints/best.pt
optim:
  lr: 5e-5 #3e-4
  weight_decay: 0.01
